{
  "scripts": {
    "dev": "concurrently \"cd frontend && next dev\" \"source backend/env-10/bin/activate && cd backend && python app.py\"",
    "build": "next build",
    "start": "next start"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "@uidotdev/usehooks": "^2.4.1",
    "axios": "^1.10.0"
  }
}
